<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hrp.auth.mapper.PositionMapper">

    <resultMap id="BaseResultMap" type="com.hrp.common.entity.Position">
        <id column="position_id" property="positionId"/>
        <result column="position_code" property="positionCode"/>
        <result column="position_name" property="positionName"/>
        <result column="is_stop" property="isStop"/>
        <result column="create_user" property="createUser"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM sys_position WHERE position_id = #{positionId}
    </select>

    <select id="selectByCode" resultMap="BaseResultMap">
        SELECT * FROM sys_position WHERE position_code = #{positionCode}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM sys_position ORDER BY create_time DESC
    </select>

    <insert id="insert" parameterType="com.hrp.common.entity.Position">
        INSERT INTO sys_position (position_code, position_name, is_stop, create_user)
        VALUES (#{positionCode}, #{positionName}, #{isStop}, #{createUser})
    </insert>

    <update id="updateById" parameterType="com.hrp.common.entity.Position">
        UPDATE sys_position
        <set>
            <if test="positionCode != null">position_code = #{positionCode},</if>
            <if test="positionName != null">position_name = #{positionName},</if>
            <if test="isStop != null">is_stop = #{isStop},</if>
        </set>
        WHERE position_id = #{positionId}
    </update>

    <delete id="deleteById">
        DELETE FROM sys_position WHERE position_id = #{positionId}
    </delete>

</mapper>

