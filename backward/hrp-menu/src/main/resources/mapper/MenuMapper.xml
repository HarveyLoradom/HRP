<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hrp.menu.mapper.MenuMapper">

    <resultMap id="BaseResultMap" type="com.hrp.common.entity.Menu">
        <id column="id" property="id"/>
        <result column="menu_code" property="menuCode"/>
        <result column="menu_name" property="menuName"/>
        <result column="parent_id" property="parentId"/>
        <result column="menu_type" property="menuType"/>
        <result column="path" property="path"/>
        <result column="component" property="component"/>
        <result column="icon" property="icon"/>
        <result column="sort" property="sort"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM sys_menu WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM sys_menu ORDER BY sort ASC
    </select>

    <select id="selectMenusByUserId" resultMap="BaseResultMap">
        SELECT DISTINCT m.*
        FROM sys_menu m
        INNER JOIN sys_user_menu um ON m.id = um.menu_id
        WHERE um.user_id = #{userId}
          AND m.status = 1
        ORDER BY m.sort ASC
    </select>

    <insert id="insert" parameterType="com.hrp.common.entity.Menu">
        INSERT INTO sys_menu (menu_code, menu_name, parent_id, menu_type, path, component, icon, sort, status, remark)
        VALUES (#{menuCode}, #{menuName}, #{parentId}, #{menuType}, #{path}, #{component}, #{icon}, #{sort}, #{status}, #{remark})
    </insert>

    <update id="updateById" parameterType="com.hrp.common.entity.Menu">
        UPDATE sys_menu
        <set>
            <if test="menuCode != null">menu_code = #{menuCode},</if>
            <if test="menuName != null">menu_name = #{menuName},</if>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="menuType != null">menu_type = #{menuType},</if>
            <if test="path != null">path = #{path},</if>
            <if test="component != null">component = #{component},</if>
            <if test="icon != null">icon = #{icon},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="status != null">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM sys_menu WHERE id = #{id}
    </delete>

</mapper>

