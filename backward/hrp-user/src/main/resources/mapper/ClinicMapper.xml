<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hrp.user.mapper.ClinicMapper">

    <resultMap id="BaseResultMap" type="com.hrp.common.entity.Clinic">
        <id column="clinic_id" property="clinicId"/>
        <result column="clinic_code" property="clinicCode"/>
        <result column="clinic_name" property="clinicName"/>
        <result column="dept_id" property="deptId"/>
        <result column="clinic_type" property="clinicType"/>
        <result column="clinic_phone" property="clinicPhone"/>
        <result column="clinic_manager_id" property="clinicManagerId"/>
        <result column="is_stop" property="isStop"/>
        <result column="create_user" property="createUser"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM sys_clinic WHERE clinic_id = #{clinicId} AND is_stop = 0
    </select>

    <select id="selectByCode" resultMap="BaseResultMap">
        SELECT * FROM sys_clinic WHERE clinic_code = #{clinicCode} AND is_stop = 0
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM sys_clinic WHERE is_stop = 0 ORDER BY clinic_code ASC
    </select>

    <select id="selectByDeptId" resultMap="BaseResultMap">
        SELECT * FROM sys_clinic WHERE dept_id = #{deptId} AND is_stop = 0 ORDER BY clinic_code ASC
    </select>

    <insert id="insert" parameterType="com.hrp.common.entity.Clinic">
        INSERT INTO sys_clinic (clinic_code, clinic_name, dept_id, clinic_type, clinic_phone, clinic_manager_id, is_stop, create_user)
        VALUES (#{clinicCode}, #{clinicName}, #{deptId}, #{clinicType}, #{clinicPhone}, #{clinicManagerId}, #{isStop}, #{createUser})
    </insert>

    <update id="updateById" parameterType="com.hrp.common.entity.Clinic">
        UPDATE sys_clinic
        <set>
            <if test="clinicCode != null">clinic_code = #{clinicCode},</if>
            <if test="clinicName != null">clinic_name = #{clinicName},</if>
            <if test="deptId != null">dept_id = #{deptId},</if>
            <if test="clinicType != null">clinic_type = #{clinicType},</if>
            <if test="clinicPhone != null">clinic_phone = #{clinicPhone},</if>
            <if test="clinicManagerId != null">clinic_manager_id = #{clinicManagerId},</if>
            <if test="isStop != null">is_stop = #{isStop},</if>
        </set>
        WHERE clinic_id = #{clinicId}
    </update>

    <delete id="deleteById">
        UPDATE sys_clinic SET is_stop = 1 WHERE clinic_id = #{clinicId}
    </delete>

</mapper>
