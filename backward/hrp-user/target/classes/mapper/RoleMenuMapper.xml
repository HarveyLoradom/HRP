<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hrp.user.mapper.RoleMenuMapper">

    <resultMap id="BaseResultMap" type="com.hrp.common.entity.RoleMenu">
        <id column="id" property="id"/>
        <result column="role_id" property="roleId"/>
        <result column="menu_id" property="menuId"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectByRoleId" resultMap="BaseResultMap">
        SELECT * FROM sys_role_menu WHERE role_id = #{roleId}
    </select>

    <insert id="insert" parameterType="com.hrp.common.entity.RoleMenu">
        INSERT INTO sys_role_menu (role_id, menu_id)
        VALUES (#{roleId}, #{menuId})
    </insert>

    <delete id="deleteByRoleId">
        DELETE FROM sys_role_menu WHERE role_id = #{roleId}
    </delete>

    <delete id="deleteByRoleIdAndMenuId">
        DELETE FROM sys_role_menu WHERE role_id = #{roleId} AND menu_id = #{menuId}
    </delete>

</mapper>
